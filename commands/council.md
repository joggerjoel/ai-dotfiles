---
description: Convene the adversarial council to audit a plan/spec/design doc before code is written
argument-hint: "[path to plan/spec/design doc, or a description of what to review]"
---

Invoke the `council` skill (via the Skill tool) to run an adversarial, multi-lens
pre-implementation review.

Target of the review: $ARGUMENTS

If no target was provided above, ask which document (path) or design should be reviewed
before proceeding. Then follow the council skill's instructions exactly.
